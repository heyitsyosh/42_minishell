command :=			<subshell>
		|			<simple_command>

subshell :=			'(' <compound_list>')'

compound_list :=	<list> '&&' <list>
			  |		<list> '||' <list>
			  |		<pipeline>

pipeline :=			<pipeline> '|' <pipeline>
		 |			<command>

simple_command :=	<word>
			   |	<redirection>
			   |	<word> <simple_command>
			   |	<redirection> <simple_command>

TO-DO 
: make all errors output to STDERR

: cd
: handle signals (ctrl-C (sigint) new prompt new line, ctrl-D exits)
: parser
: expander
: redirection
: pipe/execution